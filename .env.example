# PostgreSQL Configuration for Grafana Datasource
# These environment variables configure the Grafana PostgreSQL datasource

# PostgreSQL Host (default: postgres for Docker, localhost for local)
POSTGRES_HOST=postgres

# PostgreSQL Port (default: 5432)
POSTGRES_PORT=5432

# PostgreSQL Database Name for Metrics (default: grok_metrics)
POSTGRES_METRICS_DB=grok_metrics

# PostgreSQL User (default: postgres)
POSTGRES_USER=postgres

# PostgreSQL Password (default: password)
POSTGRES_PASSWORD=password

# PostgreSQL SSL Mode (default: disable)
POSTGRES_SSLMODE=disable

# Example configurations for different environments:

# Development (using Docker)
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_METRICS_DB=grok_metrics
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_SSLMODE=disable

# Development (using local PostgreSQL)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_METRICS_DB=grok_metrics
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
# POSTGRES_SSLMODE=disable

# Production
# POSTGRES_HOST=your-postgres-host.com
# POSTGRES_PORT=5432
# POSTGRES_METRICS_DB=grok_metrics_prod
# POSTGRES_USER=grok_user
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_SSLMODE=require